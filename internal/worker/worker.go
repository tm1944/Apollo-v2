// Package worker will define the in-memory worker record the control plane
// uses for scheduling and health (id, address, health, active requests,
// last successful health check).
//
// HEL-007: types and constructors only — no registry yet.
package worker

// Package config will hold process configuration (listen address, worker
// addresses, timeouts, health interval).
//
// HEL-001: address / port from flag or env.
// Later tickets add worker list, inference timeout, health poll interval.
package config

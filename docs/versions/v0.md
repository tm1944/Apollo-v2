# v0

The original prototype used one Python process, a global `queue.Queue`, and a single ADD function. It established the job and worker vocabulary but had no network boundary, concurrency, failure detection, retry behavior, or deployment model.

The distributed rewrite keeps the small typed-task idea and replaces the global queue with a Go control plane and streamed Python workers. See the root README for the current contract and measured behavior.

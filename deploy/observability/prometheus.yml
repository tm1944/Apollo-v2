global:
  scrape_interval: 2s
  evaluation_interval: 2s

scrape_configs:
  - job_name: control-plane
    static_configs:
      - targets: ["control-plane:9090"]
  - job_name: workers
    dns_sd_configs:
      - names: ["worker"]
        type: A
        port: 8000
        refresh_interval: 5s
